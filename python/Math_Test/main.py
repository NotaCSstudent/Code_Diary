class Math_Stuff:
    def __init__(self,x:int,y:int,z:int):
        self.x = x
        self.y = y
        self.z = z
        self.t = self.x+self.y+self.z
    def add():
        self.t = self.x + self.y + self.z
    def multiply():
        self.t = self.x * self.y * self.z


t = Math_Stuff(1,2,3)
t.add()
t.multiply()